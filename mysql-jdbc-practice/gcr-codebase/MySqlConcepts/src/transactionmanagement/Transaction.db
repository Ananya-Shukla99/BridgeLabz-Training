====================================================
        BANKING SYSTEM USING MySQL
====================================================

1. Show Databases
-----------------

mysql> SHOW DATABASES;
+--------------------+
| Database           |
+--------------------+
| bankingsystem_db   |
| employee_db        |
|                    |
+--------------------+
8 rows in set (0.03 sec)

----------------------------------------------------

2. Select Database
------------------

mysql> USE bankingsystem_db;
Database changed

----------------------------------------------------

3. Create Accounts Table
------------------------

mysql> CREATE TABLE accounts (
    -> account_id INT AUTO_INCREMENT PRIMARY KEY,
    -> name VARCHAR(100),
    -> balance DECIMAL(12,2) NOT NULL
    -> );

Query OK, 0 rows affected (0.07 sec)

----------------------------------------------------

4. Create Transactions Table
----------------------------

mysql> CREATE TABLE transactions (
    -> trans_id INT AUTO_INCREMENT PRIMARY KEY,
    -> from_account INT,
    -> to_account INT,
    -> amount DECIMAL(12,2),
    -> trans_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    -> FOREIGN KEY (from_account) REFERENCES accounts(account_id),
    -> FOREIGN KEY (to_account) REFERENCES accounts(account_id)
    -> );

Query OK, 0 rows affected (0.12 sec)

----------------------------------------------------

5. Insert Data into Accounts Table
---------------------------------

mysql> INSERT INTO accounts (name, balance)
    -> VALUES
    -> ('Rahul', 50000),
    -> ('Anjani', 40000),
    -> ('Ananya', 100000);

Query OK, 3 rows affected (0.02 sec)
Records: 3  Duplicates: 0  Warnings: 0

----------------------------------------------------

6. Create Stored Procedure: Transfer Money
------------------------------------------

mysql> DELIMITER $$

mysql> CREATE PROCEDURE transfer_money(
    -> IN sender_id INT,
    -> IN receiver_id INT,
    -> IN transfer_amount DECIMAL(12,2)
    -> )
    -> BEGIN
    -> DECLARE sender_balance DECIMAL(12,2);
    ->
    -> DECLARE EXIT HANDLER FOR SQLEXCEPTION
    -> BEGIN
    ->     ROLLBACK;
    ->     SELECT 'Transaction Failed! Rolled Back.' AS Message;
    -> END;
    ->
    -> START TRANSACTION;
    ->
    -> SELECT balance
    -> INTO sender_balance
    -> FROM accounts
    -> WHERE account_id = sender_id
    -> FOR UPDATE;
    ->
    -> IF sender_balance < transfer_amount THEN
    ->     ROLLBACK;
    ->     SELECT 'Insufficient Balance!' AS Message;
    -> ELSE
    ->
    ->     UPDATE accounts
    ->     SET balance = balance - transfer_amount
    ->     WHERE account_id = sender_id;
    ->
    ->     UPDATE accounts
    ->     SET balance = balance + transfer_amount
    ->     WHERE account_id = receiver_id;
    ->
    ->     INSERT INTO transactions (from_account, to_account, amount)
    ->     VALUES (sender_id, receiver_id, transfer_amount);
    ->
    ->     COMMIT;
    ->     SELECT 'Transaction Successful!' AS Message;
    ->
    -> END IF;
    ->
    -> END$$

Query OK, 0 rows affected (0.11 sec)

mysql> DELIMITER ;

----------------------------------------------------

7. Create Stored Procedure: Check Balance
----------------------------------------

mysql> DELIMITER $$

mysql> CREATE PROCEDURE check_balance(
    -> IN acc_id INT
    -> )
    -> BEGIN
    -> SELECT account_id,
    -> name,
    -> balance
    -> FROM accounts
    -> WHERE account_id = acc_id;
    -> END$$

Query OK, 0 rows affected (0.01 sec)

mysql> DELIMITER ;

----------------------------------------------------

8. Check Balance
----------------

mysql> CALL check_balance(1);

+------------+-------+----------+
| account_id | name  | balance  |
+------------+-------+----------+
|          1 | Rahul | 50000.00 |
+------------+-------+----------+
1 row in set (0.05 sec)

Query OK, 0 rows affected (0.05 sec)

----------------------------------------------------

9. Create Stored Procedure: Transaction History
-----------------------------------------------

mysql> DELIMITER $$

mysql> CREATE PROCEDURE transaction_history(
    -> IN acc_id INT
    -> )
    -> BEGIN
    -> SELECT
    ->     t.trans_id,
    ->     a1.name AS sender,
    ->     a2.name AS receiver,
    ->     t.amount,
    ->     t.trans_date
    -> FROM transactions t
    -> JOIN accounts a1 ON t.from_account = a1.account_id
    -> JOIN accounts a2 ON t.to_account = a2.account_id
    -> WHERE t.from_account = acc_id
    ->    OR t.to_account = acc_id
    -> ORDER BY t.trans_date DESC;
    -> END$$

Query OK, 0 rows affected (0.01 sec)

mysql> DELIMITER ;

----------------------------------------------------

10. View Transaction History
----------------------------

mysql> CALL transaction_history(1);

Empty set (0.03 sec)

Query OK, 0 rows affected (0.03 sec)

----------------------------------------------------

11. View All Accounts (Before Transaction)
------------------------------------------

mysql> SELECT * FROM accounts;

+------------+--------+-----------+
| account_id | name   | balance   |
+------------+--------+-----------+
| 1          | Rahul  | 50000.00  |
| 2          | Anjani | 40000.00  |
| 3          | Ananya |100000.00  |
+------------+--------+-----------+

----------------------------------------------------

12. Perform Money Transfer
--------------------------

Transfer ₹10,000 from Rahul (ID=1) to Anjani (ID=2)

mysql> CALL transfer_money(1, 2, 10000);

+--------------------------+
| Message                  |
+--------------------------+
| Transaction Successful!  |
+--------------------------+

1 row in set (0.06 sec)

----------------------------------------------------

13. View All Accounts (After Transaction)
-----------------------------------------

mysql> SELECT * FROM accounts;

+------------+--------+-----------+
| account_id | name   | balance   |
+------------+--------+-----------+
| 1          | Rahul  | 40000.00  |
| 2          | Anjani | 50000.00  |
| 3          | Ananya |100000.00  |
+------------+--------+-----------+

----------------------------------------------------

14. View Transactions Table
---------------------------

mysql> SELECT * FROM transactions;

+----------+--------------+------------+----------+---------------------+
| trans_id | from_account | to_account | amount   | trans_date          |
+----------+--------------+------------+----------+---------------------+
| 1        | 1            | 2          |10000.00  | 2026-02-10 11:25:40 |
+----------+--------------+------------+----------+---------------------+

1 row in set (0.04 sec)

----------------------------------------------------

15. View Transaction History (Rahul)
-----------------------------------

mysql> CALL transaction_history(1);

+----------+--------+---------+----------+---------------------+
| trans_id | sender | receiver| amount   | trans_date          |
+----------+--------+---------+----------+---------------------+
| 1        | Rahul  | Anjani  |10000.00  | 2026-02-10 11:25:40 |
+----------+--------+---------+----------+---------------------+

1 row in set (0.03 sec)

----------------------------------------------------

16. Check Balance (After Transaction)
-------------------------------------

mysql> CALL check_balance(1);

+------------+-------+----------+
| account_id | name  | balance  |
+------------+-------+----------+
| 1          | Rahul | 40000.00 |
+------------+-------+----------+

----------------------------------------------------

End of File
====================================================
